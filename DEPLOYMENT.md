# دليل النشر والاستخدام - Clickjacking Professional Lab

## 📋 المتطلبات الأساسية

قبل البدء، تأكد من توفر المتطلبات التالية:

- **متصفح حديث**: Chrome, Firefox, Edge (يدعم Web Components)
- **خادم ويب**: Python 3, Node.js, أو أي خادم HTTP آخر
- **اتصال إنترنت**: للوصول إلى البروكسيات والمكتبات الخارجية
- **أدوات التطوير**: (اختياري) VS Code أو محرر نصوص آخر

## 🚀 طرق التشغيل المختلفة

### 1. استخدام Python (الأسهل)

```bash
# الانتقال إلى مجلد المشروع
cd /path/to/clickjacking-lab

# بدء الخادم
python3 -m http.server 8000

# أو إذا كنت تستخدم Python 2
python -m SimpleHTTPServer 8000
```

ثم افتح المتصفح وانتقل إلى: `http://localhost:8000`

### 2. استخدام Node.js

```bash
# التأكد من تثبيت http-server
npm install -g http-server

# بدء الخادم
http-server -p 8000

# أو مع خيارات متقدمة
http-server -p 8000 -c-1 --cors
```

### 3. استخدام PHP

```bash
# بدء الخادم المدمج
php -S localhost:8000
```

### 4. استخدام Node.js Express (متقدم)

```bash
# إنشاء ملف server.js
cat > server.js << 'EOF'
const express = require('express');
const app = express();

app.use(express.static('.'));
app.listen(8000, () => {
    console.log('Server running on http://localhost:8000');
});
EOF

# تثبيت Express
npm install express

# بدء الخادم
node server.js
```

## 🌐 النشر على الإنترنت

### خيار 1: استخدام GitHub Pages

```bash
# تهيئة مستودع Git
git init
git add .
git commit -m "Initial commit"

# إضافة المستودع البعيد
git remote add origin https://github.com/yourusername/clickjacking-lab.git

# دفع الملفات
git push -u origin main
```

ثم فعّل GitHub Pages من إعدادات المستودع.

### خيار 2: استخدام Netlify

```bash
# تثبيت Netlify CLI
npm install -g netlify-cli

# تسجيل الدخول
netlify login

# نشر المشروع
netlify deploy --prod --dir=.
```

### خيار 3: استخدام Vercel

```bash
# تثبيت Vercel CLI
npm install -g vercel

# نشر المشروع
vercel --prod
```

### خيار 4: استخدام Heroku

```bash
# إنشاء ملف Procfile
echo "web: python3 -m http.server \$PORT" > Procfile

# تهيئة Heroku
heroku create your-app-name
git push heroku main
```

## 🔧 الإعدادات المتقدمة

### تغيير الموقع المستهدف

عدّل الملف `index.html` أو `advanced.html`:

```javascript
const CONFIG = {
    targetUrl: 'https://your-target-site.com', // غيّر هنا
    // ... باقي الإعدادات
};
```

### تغيير البروكسيات

```javascript
const CONFIG = {
    proxies: [
        { name: 'Custom Proxy 1', url: 'https://your-proxy-1.com/' },
        { name: 'Custom Proxy 2', url: 'https://your-proxy-2.com/' },
        // أضف بروكسيات إضافية
    ]
};
```

### تفعيل وضع التصحيح تلقائياً

أضف هذا السطر في بداية سكريبت التطبيق:

```javascript
CONFIG.debugMode = true; // تفعيل وضع التصحيح
```

## 🧪 الاختبار والتحقق

### اختبار الوظائف الأساسية:

1. **فتح الصفحة**: تحقق من ظهور الواجهة بشكل صحيح
2. **النقر على الزر**: تحقق من بدء التحميل
3. **فحص Console**: اضغط F12 وتحقق من عدم وجود أخطاء
4. **تفعيل Debug Mode**: اضغط F12 لعرض شريط المعلومات

### اختبار الأداء:

```javascript
// في Console
console.time('loadContent');
loadContentViaProxy(CONFIG.targetUrl);
console.timeEnd('loadContent');
```

## 📊 مراقبة الأداء

### استخدام Chrome DevTools:

1. اضغط `F12` لفتح أدوات التطوير
2. انتقل إلى تبويب **Network** لمراقبة الطلبات
3. تحقق من **Performance** لتحليل الأداء
4. استخدم **Console** لمراقبة الأخطاء

### قياس سرعة التحميل:

```javascript
// أضف هذا في Console
performance.mark('start');
loadContentViaProxy(CONFIG.targetUrl);
performance.mark('end');
performance.measure('load', 'start', 'end');
console.log(performance.getEntriesByName('load')[0]);
```

## 🔐 الأمان والخصوصية

### نصائح الأمان:

1. **لا تستخدم في الإنتاج**: هذا مشروع تعليمي فقط
2. **احم البيانات الحساسة**: لا تدخل بيانات شخصية
3. **استخدم HTTPS**: عند النشر على الإنترنت
4. **تحقق من السياسات**: تأكد من امتثال القوانين المحلية

### حماية المشروع:

```html
<!-- أضف هذا في <head> للحماية الأساسية -->
<meta http-equiv="Content-Security-Policy" 
      content="default-src 'self'; script-src 'self' 'unsafe-inline' unpkg.com">
```

## 🐛 استكشاف الأخطاء

### المشكلة: الصفحة لا تحمل

**الحل**:
- تحقق من أن الخادم يعمل
- تأكد من الرابط الصحيح
- افحص Console للأخطاء

### المشكلة: البروكسي لا يعمل

**الحل**:
- جرّب بروكسي آخر
- تحقق من اتصالك بالإنترنت
- تأكد من أن البروكسي متاح

### المشكلة: الزر لا يستجيب

**الحل**:
- افحص Console للأخطاء
- تحقق من JavaScript في المتصفح
- جرّب متصفح آخر

## 📝 السجلات والتصحيح

### عرض السجلات:

```bash
# إذا كنت تستخدم Python
python3 -m http.server 8000 --verbose

# إذا كنت تستخدم Node.js
http-server -p 8000 -d false
```

### حفظ السجلات:

```bash
# حفظ السجلات في ملف
python3 -m http.server 8000 > server.log 2>&1 &

# عرض السجلات
tail -f server.log
```

## 🚦 حالات الاستخدام

### للأغراض التعليمية:

1. شرح مفاهيم الأمان السيبراني
2. توضيح تقنيات Clickjacking
3. دراسة تجاوز قيود الأمان
4. فهم CORS والبروكسيات

### للأغراض البحثية:

1. اختبار الثغرات الأمنية (مع الإذن)
2. دراسة تقنيات الدفاع
3. تحليل أنماط الهجوم
4. تطوير حلول أمنية

## 📚 مراجع إضافية

- [OWASP Security Testing Guide](https://owasp.org/www-project-web-security-testing-guide/)
- [MDN Web Docs - Security](https://developer.mozilla.org/en-US/docs/Web/Security)
- [PortSwigger Web Security Academy](https://portswigger.net/web-security)
- [GitHub - x-frame-bypass](https://github.com/niutech/x-frame-bypass)

## ✉️ الدعم والمساعدة

للحصول على الدعم:

1. تحقق من ملف README.md
2. ابحث عن الحل في الأسئلة الشائعة
3. تواصل عبر البريد الإلكتروني
4. أنشئ issue على GitHub

---

**ملاحظة مهمة**: هذا المشروع تعليمي فقط. استخدمه بمسؤولية وأخلاقية.
